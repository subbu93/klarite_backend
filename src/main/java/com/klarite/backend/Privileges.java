package com.klarite.backend;

import java.util.Arrays;
import java.util.HashSet;

public class Privileges {
    public static final HashSet<String> skillObserver = new HashSet<>(Arrays.asList("Site Administrator", "Supervisor", "Super User"));
}